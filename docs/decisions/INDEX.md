# Decisions index

Empty by design: no pivot decision (a choice that redirected the architecture or foreclosed an alternative) has been made yet. See `.claude/skills/solution-architecture/SKILL.md` (`PIVOT_OR_RULE_ONLY`) — routine technical choices are never recorded here.

Add one line per decision, linking to its record filed at the path of the module/area it governs.
