// Boundaries owned by the agent loop: how long it may run and how much
// tool output it may feed back into the model.

export const MAX_AGENT_ITERATIONS = 10;
export const DEFAULT_AGENT_ITERATIONS = 5;
export const MAX_TOOL_OUTPUT_CHARS = 8000;
