// The extension's released version. package.json and manifest.json cannot
// import JavaScript, so they each carry their own copy; reqs/cross/version.cross.spec.js
// asserts all three agree.

export const EXTENSION_VERSION = '1.1.0';
