// Boundaries owned by the template domain. Imported by the app, the
// examples, and the specs — never restated at any of those.

export const MAX_TEMPLATES = 50;
export const MAX_TEMPLATE_NAME_LENGTH = 50;
export const MAX_TEMPLATE_DESCRIPTION_LENGTH = 200;
export const MAX_TEMPLATE_PROMPT_LENGTH = 2000;
