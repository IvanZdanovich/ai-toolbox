# Requirements index

Empty by design: no business rule (a constraint the domain/product imposes, not a technical implementation detail) has been captured yet. See `.claude/skills/solution-architecture/SKILL.md` (`PIVOT_OR_RULE_ONLY`).

Add one line per area, linking to its `<area>.constraints`/`<area>.examples`/`<area>.spec` trio.
